module.exports = () => {
  /**
   * import axios
   * import books.json
   * get google URL
   * google key
   * loop thru books.json
   * get book title (from books)
   * get author name (from books)
   * create GET url from google URL, title, author name,
   * send request with URL
   */
};
